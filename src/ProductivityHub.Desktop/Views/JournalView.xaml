<UserControl x:Class="ProductivityHub.Desktop.Views.JournalView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="📔 Daily journal" Style="{StaticResource Heading}" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="240" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Date:" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <DatePicker x:Name="DateBox" Width="130" SelectedDateChanged="Date_Changed" />
                        <TextBlock Text="Mood:" Style="{StaticResource Muted}" VerticalAlignment="Center" Margin="14,0,6,0" />
                        <TextBox x:Name="MoodBox" Width="150" />
                    </StackPanel>
                    <Button DockPanel.Dock="Bottom" Content="Save entry" Style="{StaticResource AccentButton}"
                            Click="Save_Click" HorizontalAlignment="Left" Margin="0,8,0,0" />
                    <TextBox x:Name="BodyBox" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" VerticalContentAlignment="Top" />
                </DockPanel>
            </Border>

            <DockPanel Grid.Column="1">
                <TextBlock DockPanel.Dock="Top" Text="Recent" Style="{StaticResource Muted}" Margin="0,0,0,6" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="RecentHost">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource Card}" Margin="0,0,0,6" Padding="10,8"
                                        Cursor="Hand" MouseLeftButtonUp="Recent_Click">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                                        <TextBlock Text="{Binding Preview}" Style="{StaticResource Muted}" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
